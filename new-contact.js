document.addEventListener("DOMContentLoaded", function () {
    const saveuserbutton = document.getElementsByClassName("save");

    // Add event listener to the save new user button
    //document.addEventListener("click", function (event) {
       // if (event.target === adduserbutton[0]) {
           // window.location.href = "add-user.html";
       // }
    //});
});